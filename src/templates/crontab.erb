PATH=/opt/gomicro/sbin:/opt/gomicro/bin:/data/sbin:/data/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
<%
  crons.each do |job|
-%>
<%= sanitize_cron(job[:schedule]) %> root /opt/microbox/cron/<%= job[:id] %>
<% 
  end
-%>